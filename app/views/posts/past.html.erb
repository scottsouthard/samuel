<h2 class="list-head"> Past Shows </h2>
<div class="gallery-line">
</div>
<ul class="list-group post-index post-list">
  <% @posts.each do |post| %>
      <li >
        <%= link_to "#{post.title}  -  #{post.date.strftime("%m/%d/%Y")}", post%>
      </li>
  <% end %>
</div>
