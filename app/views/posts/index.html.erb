<div class="container">
  <div class="list-group post-index">
  <% @posts.each do |post| %>
      <!-- <li class="list-group-item"> -->
        <%= link_to "#{post.title}  -  #{post.date.strftime("%m/%d/%Y")}", post, :class=>"list-group-item"%>
      <!-- </li> -->
  <% end %>
  </div>
</div>
<%= link_to "New Post", new_post_path %>
